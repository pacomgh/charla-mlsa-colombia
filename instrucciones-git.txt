echo "# charla-mlsa-colombia" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/pacomgh/charla-mlsa-colombia.git
git push -u origin main

git config --global user.name "mi nombre"
git config --global user.name "correo@loquesea.com"
